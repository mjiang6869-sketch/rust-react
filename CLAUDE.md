## Subagent 使用规则

目标：**规划用 Opus 5.5（`architect`），写码用 Sonnet（`coder`），二者配合；不要重复创建 subagent。**

### 现有 subagent 注册表（唯一事实来源）

| name | 模型 | 职责 | 何时使用 |
| --- | --- | --- | --- |
| `architect` | Opus 5.5 | 只读规划：现状 → 方案 → 分步改动清单 → 验收标准 | 多文件改动、架构决策、需求不明确时，第一步 |
| `coder` | Sonnet | 按已确认的计划写代码并跑验证，一次一小步 | 计划明确后的实现 |
| `Explore`（内置） | 继承 | 快速只读检索 | 找代码、查引用 |

判断「是否已有类似 agent」**只看上表**，不要重新 grep、不要为单次任务临时创建。

### 默认协作流程

1. **先规划后编码**：改动 ≥2 个文件、涉及设计决策或需求含糊时，第一步必须派 `architect` 出计划；
2. 主线程把计划要点复述给用户，确认后再派 `coder` 实现；一次一步，每步都有可执行的验收命令；
3. **验证由主线程做**：亲自跑测试/命令、查 diff，不采信「已完成」的口头结论；
4. `coder` 发现计划与代码冲突时停止并回报，由主线程决定是否让 `architect` 修订计划。

角色不可互换：`architect` 不改代码，`coder` 不做方案设计。

### 禁止行为

- **未经用户明确同意，禁止新建或修改 `.claude/agents/` 下任何文件**（项目 settings 已用 ask 规则拦截；被拦下时向用户说明，不要绕过）；
- 禁止重复创建功能重叠的 subagent；确实需要新能力时：对照上表 → 现有覆盖 ≥80% 即复用 → 否则先说明理由、征得同意后再建，并登记进上表；
- 禁止 subagent 再派生 subagent（`CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH=1` 已强制，两个自定义 agent 也没有 Agent 工具）；
- 同一类任务并行 subagent 不超过 3 个（`CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS=3` 已强制）；
- 禁止跳过规划直接大改（小修除外），禁止跳过验证宣布完成。

### 派发要求

- 派 `architect`：目标 + 已知约束 + 期望产出粒度；
- 派 `coder`：计划全文（或明确步骤）+ 验收标准 + 本次只做哪一步。