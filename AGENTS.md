# Rust Crypto AI Agent 工作约定

本文件适用于 `rust-crypto` 根目录及其全部子目录。它是给 AI 编码代理的项目说明与执行约束。

## 项目用途

币安 USDⓈ-M 永续合约的做市研究平台。当前的核心动机是币安的活动费率：
**USDC 本位合约 maker 零手续费，TradFi 合约 maker 零手续费**。零手续费消除了
成本项，但不产生 edge——真正的 edge 必须来自「挂单成交后价格没有立刻反向走」。

除回测与模拟盘外，本项目还要解决一件币安原生做不到的事：**一个点位挂单、
同时挂分批止盈与止损**。币安 USDⓈ-M 没有 OCO，也没有 bracket 单，支持的条件单
只有 `STOP / STOP_MARKET / TAKE_PROFIT / TAKE_PROFIT_MARKET / TRAILING_STOP_MARKET`，
一张单只能对应一个数量与一个触发价。所以以下四件事必须由客户端实现，也是这个
软件存在的理由：

1. 一个点位挂单，同时挂分批止盈与止损
2. 分批止盈（例如 40%/30%/30%），三张单分开挂、分别成交
3. 保本止损：浮盈后把止损推到入场价，需要撤单重挂
4. 止损成交后撤销未成交的止盈单

## 目录结构与依赖方向

```
crates/
├── domain/       零 I/O 领域层：量化、合约规则、行情、策略接口、风控、
│                 订单状态机、保护单规划器、持仓-保护单一致性、运行模式
├── data/         历史数据：定点编码、下载台账、Parquet 转换、回放、下载器
├── sim/          撮合与回测：成交模型 M0/M1、成交带、反欺指标、回测引擎
├── store/        热状态持久化：SQLite schema、迁移、订单与历史仓储
├── strategies/   策略实现
├── exchange/     币安客户端：签名、合约规则解析、REST 客户端
└── cli/          命令行入口
```

依赖方向严格向内：`cli` → `strategies` / `exchange` / `store` / `sim` / `data` → `domain`。

**`domain` 的零 I/O 是硬约束**，CI 校验：

```sh
cargo tree -p domain | grep -E 'tokio|reqwest|duckdb|axum'   # 必须无输出
```

这条约束换来的能力：回测、模拟盘、实盘共用同一套订单生命周期与保护单数学，
全部可用纯函数测试覆盖，无需 fixture、网络或 tokio runtime。

## 必须守住的不变量

### 交易数值与精度

- 金额、价格、数量、费率、保证金一律用 `rust_decimal::Decimal`。
  **`f32`/`f64` 在编译期被 `clippy.toml` 的 `disallowed-types` 禁止。**
- 时间统一 UTC。SQLite 中存毫秒整数，序列化时用字符串。
- 结算资产必须显式记账。`wallet.usdt` 与 `wallet.usdc` 绝不合并成无标识的数字。
  多资产模式下 USDT 可为 USDC 合约提供保证金，但盈亏仍结算在合约的
  `margin_asset`，两者语义不同。

### 价格量化的方向不由调用方决定

`Precision::price_for` 通过 `PriceRole`（`PassiveEntry` / `TakeProfit` / `StopLoss`）
与订单方向唯一确定舍入方向。**不要在任何地方手写量化逻辑。**

止盈与止损在同一订单方向上的取整是**相反**的（止盈多赚一跳，止损早出一跳），
这不是笔误，见 `crates/domain/src/precision.rs` 的契约表。

### 保护单与持仓的一致性

> **所有在挂的 reduce-only 单数量之和，不能超过当前持仓量。**

分批止盈容易破坏这条：第一档成交平掉 40% 后，止损单还挂着原始数量；等止损
触发时它要平的数量超过剩余持仓，币安会以 reduce-only 拒绝——**止损失效、
仓位裸露**。所有对保护单的调整必须经 `domain::position_set`，它保证这条不变量。

各档止盈的数量基准是**入场时的原始持仓量**，不是逐次递减后的持仓。

### 实盘安全

- 模式必须显式为 `PAPER` 或 `LIVE`，默认 `PAPER`。禁用一切模糊解析。
- 实盘下单要求三个条件同时成立：用户数据流已连接、账户已对账、显式 ARM
  （`domain::LiveSafety`）。数据流断线与对账不一致会**自动解除武装**；
  重启后不恢复武装状态。
- endpoint 白名单硬编码，只有币安官方生产与测试网域名。每个签名请求前重新校验。
- 止损、止盈**全部使用 reduce-only 的 GTX 限价单**。不得用市价单兜底。
- 币安条件单有 `triggerProtect`：触发价距标记价不足 5% 会被拒单。做市的止损
  常在近价位，应改用限价单挂出。见 `exchange::check_trigger_protect`。
- 网络超时与 5xx 属于「状态未知」，**必须先查询对账再决定**，绝不能直接重试提交。
- 币安错误码 `-5022`（post-only 会立即成交）是常态而非异常，且这类订单不记入
  历史、不推送事件——不要试图通过查询确认它存在过。
- 任何异常只能向更安全的状态切换：暂停开仓、只减仓、撤销开仓单、停用。
  不能因为重连或重启自动恢复风险状态。

### 成交模型的诚实性

零手续费下单笔毛利润就是止盈距离（约 4bp），所以**成交模型的保真度直接决定
策略是否「看起来」赚钱**。一个乐观 10% 的成交假设能让任何策略盈利。

- `M0WickTouchFull` 是上界，**绝不能作为默认值**，只能用于与 M1 对比。
- `M1TradeThroughQueue` 是诚实基线：必须在我们的价位上有真实成交，且成交由
  正确方向的主动单驱动。
- **排队位置无法从币安任何免费归档还原**（`aggTrades` 与 `trades` 都只有
  `is_buyer_maker`，没有订单 ID；`bookTicker` 已于 2024-04 停更）。所以 M1 的
  「排在队尾」是唯一可能的选择，不是保守近似；改进只能靠自采实时深度。
- 每次回测必须同时跑 M0 与 M1 并报告差异。**不允许只查看 M0 的结果。**
- 回测结果必须带 `sign_flips`、`breakeven_fill_rate`、`adverse_markout_5s`、
  `fee_incomplete` 等结论可信度字段。这些不是可选元信息。

### 费率诚实

整个 edge 依赖零费率活动。要求：

- 费率按合约来自 `FeeSchedule` 且带 `source`。
- `FeeSource::PromotionalAssumed` 必须让回测结果标记为 `incomplete`。
- 每份回测都报告「0% 费率 vs 常规费率」两组 P&L。

## Rust 代码规范

- 稳定版 Rust、`edition = "2024"`。提交前运行 `cargo fmt --check` 与
  `cargo clippy --all-targets -- -D warnings`。
- 禁止 `#[allow(...)]` 掩盖问题。确需例外时，在代码附近写明原因与影响。
  当前的唯一豁免为零，若将来新增必须有解释。
- 生产路径避免 `unwrap()`/`expect()`。只有能由类型或本地不变量严格保证的
  内部条件才可 `expect()`，并在附近说明原因。
- 网络重连必须有退避与超时。不能在循环中无间隔打 REST。
- 持久化结构变更时同时更新加载、保存与迁移逻辑，并增加重启测试。
  未知版本必须**报错而非重置**。
- 新职责优先拆到职责匹配的 crate。不要让 `cli` 或任何入口文件堆积业务逻辑。

## 测试要求

常用命令：

```sh
cargo fmt --check
cargo clippy --all-targets -- -D warnings
cargo test
cargo tree -p domain | grep -E 'tokio|reqwest|duckdb|axum'   # 必须无输出
```

改动以下内容时必须补测试：

- 保护单数学（分批止盈、保本、移动止损、定时取消）→ 表驱动测试，每个行为一行
- 持仓-保护单一致性 → 断言「第一档成交后止损数量同步」
- 成交模型 → 手算场景，且断言 M0 与 M1 的**符号差异**
- 价格量化 → tick 边界双向往返、三种 `PriceRole`、幂等性
- 订单状态机 → 含 `Unknown` 的唯一出口是查询
- SQLite 迁移 → 未知版本拒绝、外键生效、金额列必须是 TEXT

## 文件、配置与依赖

- 配置走环境变量。不要创建 `*.example` 或 `.env.example`。
- 密钥不写入仓库、不打印日志、不进入错误信息。`Credentials` 的 `Debug`
  是手工实现的，不要给它加 `#[derive(Debug)]`。
- 不提交 `target/`、`node_modules/`、`dist/`、`data/`、运行时状态。

## Git 提交规范

- 提交前运行 `git status --short --branch`、`git diff --check` 与相关测试。
- 提交信息使用中文，说明**为什么**而不只是做了什么。
- 不改写历史，不强制推送，不删除远端分支。
- 不回滚用户已有的未提交修改。

## 完成标准

交付前确认：

1. 说明改动的文件、行为，以及是否影响交易安全不变量。
2. 记录实际运行的测试命令与结果，未覆盖的风险要说明。
3. 检查 diff，没有新增凭据、示例配置或无关格式化。
4. 明确当前是否仍为模拟盘；任何真实下单能力都需要单独任务与明确指令。
