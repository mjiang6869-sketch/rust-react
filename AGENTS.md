# Rust Crypto AI Agent 工作约定

本文件适用于 `rust-crypto` 根目录及其全部子目录。它是给 AI 编码代理的项目说明和执行约束，目的是让后续修改保持可审查、可恢复，并且不会因为误解合约类型、结算资产或手续费而扩大交易风险。

## 项目用途

这是一个 Rust + React 的币安 USDⓈ-M 永续合约做市研究项目。当前版本是本地模拟盘：使用币安公开行情复现 Python 项目的“15 分钟区间突破失败后回踩挂单”策略，模拟开仓、Maker 止盈和限价止损。当前代码不保存 API Key，不签名请求，也不提交真实订单。

主要目录：

- `src/feed.rs`：交易规则、历史 K 线、WebSocket 行情和 REST 兜底。
- `src/signal.rs`：回踩信号和风险过滤。
- `src/paper.rs`：模拟账户、资产、订单、持仓和保护单状态机。
- `src/storage.rs`：原子状态保存和单进程锁。
- `src/main.rs`：Rust API 和行情任务。
- `frontend/src/`：React 控制台，只负责配置、状态展示和模拟盘操作。

后续如果加入真实交易，必须把交易所适配、账户对账、用户数据流和模拟盘逻辑分开。任何真实下单能力都要经过独立任务、针对性测试和明确的用户指令，不能因为“顺手完善”而加入。

真实交易必须使用显式的 `PAPER` / `LIVE` 模式和交易所 endpoint allowlist。禁止用一个环境变量或默认值让服务从模拟盘无提示地切到主网；`LIVE` 启动前必须确认账户对账、用户数据流、实时报价规则和费率读取均已就绪。

## 资产与合约语义

以下字段不能混用：

- `quote_asset`：合约计价资产，例如 `ETHUSDC` 的 `USDC`。
- `margin_asset`：合约结算和盈亏资产，必须从交易所规则读取。
- `wallet.usdt` 与 `wallet.usdc`：分别记录余额，不得合并成一个无资产标识的数字。
- `contract_type`：普通永续使用 `PERPETUAL`，TradFi 使用 `TRADIFI_PERPETUAL`。

USDT 作为 USDC 合约共享保证金只允许在显式的多资产模式语义下模拟，并且真实币安规则需要全仓、账户模式、抵押品折算和资产限制共同确认。模拟盘的 USDT/USDC 1:1 估值不能直接当成真实账户的可用保证金。生产代码禁止把两个 Decimal 余额直接相加，必须读取账户模式、抵押品折算、可用余额和清算规则。

订单、手续费和 PnL 在整个状态链路中都要带结算资产。TradFi 合约的 Maker 费率、Taker 费率、资金费、最大杠杆、交易时间和结算资产都必须按当前交易所元数据或账户实际费率处理。不得仅凭 `TRADIFI_PERPETUAL` 字符串把真实 Maker 费率设为零；当前模拟初值可以按研究假设填写，但必须明确标记为模拟初值和数据来源。

## 交易安全规则

- 默认保持模拟盘关闭状态；不要在启动、测试或迁移时自动开启策略。
- 不得读取、打印、提交或写入 API Key、Secret、Cookie、钱包凭据或本地 `.env` 内容。
- 不得把 API 服务绑定到公网地址；当前默认只允许 `127.0.0.1`。
- 日志不得打印请求 headers、签名参数、查询字符串中的密钥、Cookie 或完整账户凭据。
- 不得在没有用户明确指令的情况下加入真实下单、撤单、转账、改杠杆或切换主网的代码。
- 开仓只在行情新鲜且风险校验通过时允许；行情超过 15 秒未更新时暂停开仓，断线恢复后先补历史和对账，再恢复策略。
- 任何异常只能向更安全的状态切换：暂停开仓、只减仓、撤销开仓单、停用。不能因为重连或重启自动恢复风险状态。
- 订单必须有可追踪的唯一标识。超时或网络错误后先查询状态，再决定是否重试，不能直接重复提交。
- 止损、止盈、平仓和撤单的行为必须有测试覆盖；不能用市价兜底掩盖限价保护未成交的问题。

## Rust 代码规范

- 使用稳定版 Rust、`edition = "2024"`，提交前运行 `cargo fmt --check` 和 `cargo clippy --all-targets -- -D warnings`。
- 金额、价格、数量、费率和保证金使用 `rust_decimal::Decimal`，禁止使用 `f32` 或 `f64` 承载交易数值。
- 时间统一使用 UTC 的 `DateTime<Utc>`；只有界面展示时才转换时区。
- 网络、文件和交易所响应必须返回可处理的错误；生产路径避免 `unwrap()`、`expect()` 和静默吞错。只有能由类型或本地不变量严格保证的内部条件才可使用 `expect()`，并在代码附近说明原因。
- 不得用 `#[allow(...)]`、删除 lint、宽泛 `catch` 或 `unsafe` 掩盖问题。确需例外时，先在交付说明中写明原因和影响。
- 交易所返回的价格和数量先按交易规则的 `tickSize`、`stepSize`、`minQty`、`minNotional` 校验或量化，再进入订单状态机。
- 网络重连必须有退避、超时和状态恢复；不能在循环中无间隔地打 REST 请求。
- 状态文件使用原子替换和单进程锁。修改持久化结构时同时更新加载、保存和迁移兼容逻辑，并增加重启测试。
- 状态 JSON 应包含 `schema_version`。未知版本、损坏状态、保护单缺失或对账不一致时，进入只减仓或停机并告警，禁止静默清空状态或重新建立持仓。
- 新职责优先拆到职责匹配的模块。不要继续向已经很大的入口或状态机文件堆放无关功能。

## React 与 TypeScript 规范

- 遵守 `frontend/tsconfig.json` 的严格检查；禁止 `any`、`@ts-ignore`、`eslint-disable` 和未解释的类型断言。
- 用户可见文案使用中文，资产、合约类型和费率名称必须明确，不能只显示颜色或缩写。
- 表单字段写清单位和作用；异步保存、停用、行情断开和错误状态都要给出可读反馈。
- 交互控件要有键盘焦点、可读标签和明确的禁用状态；图标按钮需要 `aria-label` 或可见文字。
- 资产、订单、持仓表格保持稳定列宽和响应式布局，不让长订单号或价格造成页面横向溢出。
- 前端只调用已存在的 Rust API 契约。修改字段、枚举或响应结构时，同时更新类型、调用处和相关测试。

## 策略与数据修改

修改信号规则、价格量化、库存、手续费、止盈止损或熔断条件时：

1. 先阅读对应的 Python 实现和现有 Rust 测试，明确要保持的行为。
2. 为新行为增加一个能复现风险或边界的测试，而不是只测试实现细节。
3. 使用固定的 UTC K 线输入验证信号、入场价、止损价、过期时间和重复信号处理。
4. 如果改变 API 或状态 JSON，检查旧状态文件能否安全加载；无法兼容时要给出明确错误，不得静默重置持仓。
5. 不用真实币安账户作为单元测试依赖。网络集成检查必须只读，并且要有超时和失败时的安全行为。

## 测试与验证

默认只运行和改动直接相关的最小检查，不把未运行的全量检查写成已验证。常用命令：

```sh
cargo fmt --check
cargo clippy --all-targets -- -D warnings
cargo test
```

```sh
cd frontend
pnpm typecheck
pnpm build
```

改动行情、订单状态或持久化时，至少覆盖行情过期、断线恢复、重复提交、状态重启、USDT/USDC 资产归属和 TradFi 合约识别。改动页面时，检查错误、加载、空仓、持仓、停用和移动宽度布局。

## 文件、配置和依赖

- 不创建 `*.example`、`.env.example` 或用于绕过配置加载的副本文件。配置应写入项目实际使用的配置入口，并遵循敏感信息处理规则。
- 新增依赖前先查现有依赖和标准库能力，说明用途、版本和替代方案；不要为了一个调用点引入抽象或第三方库。
- 不要提交 `target/`、`node_modules/`、`dist/`、运行时状态和 `.DS_Store`。
- 读取仓库文件时优先使用 `rg`、`rg --files` 和定向读取，不要为了小改动重排无关代码。

## 服务运行与重启

修改影响 Rust API 或 Vite 时，先检查已有进程和端口，再按项目文档重启。重启后检查：

- `GET /api/health` 返回 `ok`；
- `GET /api/state` 能返回当前交易对、合约类型、资产和行情新鲜度；
- 日志没有未处理 panic、重复运行锁错误或连续无退避请求；
- 前端根页面返回 200，且 API 代理仍指向本地 Rust 服务。

## Git 提交规范

- 提交前运行 `git status --short --branch`、`git diff --check` 和针对性测试，只提交当前任务涉及的文件。
- 提交信息必须使用中文，格式简洁，例如：`增加 USDC 多资产模拟保证金`、`修复行情断线后的安全停机`。
- 不改写历史，不强制推送，不删除远端分支。推送使用仓库已有的远端和当前分支。
- 如果仓库没有远端、认证失败或分支保护阻止推送，必须明确说明失败阶段，不得声称已经推送。
- 不回滚或覆盖用户已有的未提交修改；发现同一文件存在外部改动时，先阅读并在其基础上修改。

## 完成标准

交付前确认：

1. 说明本次改动的文件、行为和资产/合约影响。
2. 记录实际运行的测试命令、工作目录、结果和未覆盖风险。
3. 检查 diff，没有新增凭据、示例配置、运行时状态或无关格式化。
4. 服务受影响时已自动重启并检查健康状态。
5. 已创建中文 Git 提交；远端不可用时已说明阻塞原因。
6. 仍然明确当前是否只有模拟盘，任何真实交易能力是否需要额外任务和审批。
